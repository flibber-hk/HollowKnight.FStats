namespace FStats
{
    public enum InitializationState
    {
        NewGame,
        ExistingGame,
    }

    public enum SettingType
    {
        Never,
        Always,
        ThisSession,
    }
}
